[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "laclaugpt-data-analysis"
version = "0.1.0"
description = "Storage-neutral analysis core for the modular LaclauGPT research framework"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "EUPL-1.2"}
authors = [{name = "Tomi Toivio"}]
dependencies = [
  "pydantic>=2.7,<3",
]

[project.optional-dependencies]
analysis = [
  "pandas>=2.2",
  "scikit-learn>=1.5",
  "statsmodels>=0.14",
]
nlp = [
  "spacy>=3.7",
  "sentence-transformers>=3.0",
  "transformers>=4.44",
]
topics = [
  "bertopic>=0.16",
  "gensim>=4.3",
]
remote = [
  "pymongo>=4.8",
  "redis>=5.0",
  "boto3>=1.35",
]
dev = [
  "pytest>=8.3",
  "pytest-cov>=5.0",
  "ruff>=0.6",
]
all = [
  "pandas>=2.2",
  "scikit-learn>=1.5",
  "statsmodels>=0.14",
  "spacy>=3.7",
  "sentence-transformers>=3.0",
  "transformers>=4.44",
  "bertopic>=0.16",
  "gensim>=4.3",
  "pymongo>=4.8",
  "redis>=5.0",
  "boto3>=1.35",
  "pytest>=8.3",
  "pytest-cov>=5.0",
  "ruff>=0.6",
]

[tool.hatch.build.targets.wheel]
packages = ["src/laclaugpt_data_analysis"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"
# src/ on the path so tests import the package without an editable install;
# repo root so `tests.fixtures` style imports resolve locally even when a
# foreign top-level `tests` package exists in site-packages.
pythonpath = ["src", "."]

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.coverage.run]
source = ["laclaugpt_data_analysis"]
branch = true
