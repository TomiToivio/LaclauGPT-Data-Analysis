Analyse frame {frame_id} at {timestamp_seconds} seconds.

[PROJECT PROFILE NOTE]
{project_note}

[TASK]
Extract usernames/handles and other visible text when supported. Treat uncertain visual identifications as uncertain. Candidate signifiers are provisional only. Do not perform final discourse classification at frame level.
