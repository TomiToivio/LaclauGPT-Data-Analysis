# Hungary26 reprocessing on CSC Roihu (ARM64/GH200)
# Install the package itself with: pip install -e .
# System dependencies such as ffmpeg and Ollama are intentionally not pip packages.
openpyxl>=3.1,<4
PyYAML>=6.0,<7
pydantic>=2.7,<3
numpy>=1.26,<3
ollama>=0.6,<1
# Validation requested by the runbook
pytest>=8.3,<9
